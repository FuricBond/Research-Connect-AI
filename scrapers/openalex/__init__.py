"""
scrapers.openalex — OpenAlex ingestion package.

Modules
-------
models          — Normalised internal data models (NormalizedWork, …)
response_models — Lightweight structural mirrors of the OpenAlex JSON response
abstract_utils  — Abstract text reconstruction from inverted index
normalizer      — OpenAlex JSON → normalised internal models
validator       — Validation of normalised models
client          — HTTP client / API access layer
"""
