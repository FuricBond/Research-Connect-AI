# Initial API

Base URL in development:

```text
http://localhost:8000
```

## Endpoints

```text
GET /api/health
GET /api/opportunities
```

The first implementation returns sample data. The next step is to connect these endpoints to PostgreSQL.
